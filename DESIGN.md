# 화면 디자인 기준

이 문서는 `references/screens/`의 `home.png`, `detail.png`, `cart.png`에서
직접 확인한 화면과 첨부 이미지에서 확인한 색상 기준이다.
화면에서 정확히 확인할 수 없는 값은 비워 둔다.
상품 사진과 상품 문구는 이 문서의 대상이 아니며 `SHOP.md`를 따른다.

## 전체 인상

- 한국 대형 쇼핑몰의 정보 밀도가 높은 화면이다.
- 장식보다 상품 정보, 가격, 선택 상태가 먼저 보인다.
- 배경은 밝고, 영역은 얇은 선으로 나눈다.
- 기본 정렬은 왼쪽 정렬이며, 가격과 단계 표시는 굵게 강조한다.
- 화면에서 확인되지 않은 그림자, 그라데이션, 애니메이션은 사용하지 않는다.
- 탐색 화면은 Behance처럼 여백이 넉넉하고 콘텐츠 카드가 반복되는 편집형 구성을
  참고한다. 단, 우리 쇼핑몰의 상품 정보와 구매 흐름을 우선한다.

## 색상

색상은 화면에서 맡은 역할별로 하나씩 정한다. 로고의 여러 색은 로고 자체의
표현에만 해당하므로 공통 UI 색상으로 사용하지 않는다.

| 역할 | 색상 값 | 쓰이는 곳 | 확인 화면 |
|---|---|---|---|
| 기본 배경 | `#ffffff` | 상품·상세·장바구니의 주 콘텐츠 배경 | 3장 모두 |
| 전체 바깥 배경 | `#f3f3f3` | 장바구니 화면 좌우 여백 | `cart.png` |
| 기본 본문색 |  | 상품명, 설명, 제목, 일반 텍스트 | 3장 모두 |
| 파란 강조색 |  | 링크, 선택 상태, 검색창 테두리, 장바구니 선택 버튼, 진행 단계 | 3장 모두 |
| 연한 구분선 |  | 헤더·콘텐츠 경계, 상품 행 구분, 주문 요약 구분 | 3장 모두 |
| 기본 테두리 |  | 카드, 수량 조절 상자, 입력·버튼 외곽선 | 3장 모두 |
| 가격 강조색 |  | 판매 가격과 금액 강조 | `home.png`, `detail.png`, `cart.png` |
| 배송/도착 안내색 |  | 배송일, 도착 예정 문구 | `home.png`, `detail.png`, `cart.png` |
| 선택 불가 버튼 배경 |  | 상품을 선택하지 않은 장바구니의 주문 버튼 | `cart.png` |
| 로고 갈색 |  | 로고의 `cou` 부분 | 3장 모두 |
| 로고 주황색 |  | 로고의 `p` 부분 | 3장 모두 |
| 로고 연두색 |  | 로고의 `a` 부분 | 3장 모두 |
| 로고 파란색 |  | 로고의 `ng` 부분 | 3장 모두 |
| 보조 라벤더 | `#f4f0fb` | 정보 패널·강조 배경 | 첨부 이미지 |
| 보조 크림 | `#fff6e3` | 상품·설명 영역의 따뜻한 배경 | 첨부 이미지 |
| 핵심 보라색 | `#4b00a5` | 링크, 선택 상태, 핵심 제목과 강조 | 첨부 이미지 |
| 보조 노랑 | `#ffb800` | 포인트 영역과 시선 유도 | 첨부 이미지 |
| 강한 본문색 | `#111111` | 본문, 제목, 아이콘, 테두리 | 첨부 이미지 |
| 연보라 보조색 | `#d9c8f0` | 선택 상태의 보조 면과 배경 | 첨부 이미지 |
| 입력 배경 | `#f4f4f7` | 검색·입력 영역 | 첨부 이미지 |
| 분홍 포인트 | `#ed4b86` | 핵심 행동 버튼과 선택 강조 | 첨부 이미지 |
| 콘텐츠 기본 배경 | `#ffffff` | 넓은 페이지와 카드 주변의 여백 | Behance 참고 화면 |
| 탐색 본문색 |  | 제목, 상품명, 탐색 메뉴 | Behance 참고 화면 |
| 탐색 보조선 |  | 검색창·카드·콘텐츠 영역 구분 | Behance 참고 화면 |

## 글꼴

| 항목 | 값 | 쓰이는 곳 |
|---|---|---|
| 글꼴 이름 | `Noto Sans KR` | 화면의 모든 한글·영문 텍스트 |
| 기본 글꼴 크기 |  | 일반 상품·설명·보조 정보 |
| 작은 글꼴 크기 |  | 배송 배지, 단위 가격, 보조 안내 |
| 중간 글꼴 크기 |  | 상품명, 선택 항목, 장바구니 상품 정보 |
| 큰 글꼴 크기 |  | 페이지 제목, 장바구니 제목 |
| 가격 글꼴 크기 |  | 상품 판매 가격, 주문 금액 |
| 일반 글꼴 굵기 | `400` | 일반 설명과 보조 정보 |
| 강조 글꼴 굵기 | `600–700` | 제목, 가격, 선택된 단계 |

화면에서 보이는 특징: 제목과 가격은 주변 정보보다 굵고 크게 표시한다. 상품
설명과 보조 정보는 상대적으로 작고 가볍다. 정확한 글꼴 이름과 각 크기·굵기는
이미지에서 확정할 수 없어 크기 값은 비워 둔다. 한글과 영어는 모두 `Noto Sans KR`
을 사용해 글꼴 인상이 분리되지 않도록 한다.


## 여백과 크기

| 항목 | 값 | 확인 화면 |
|---|---:|---|
| 전체 콘텐츠 좌우 여백 |  | 3장 모두 |
| 헤더 높이 |  | `home.png`, `detail.png` |
| 상품 목록 카드 사이 간격 |  | `home.png` |
| 상세 화면 이미지와 정보 사이 간격 |  | `detail.png` |
| 장바구니 제목과 본문 사이 간격 |  | `cart.png` |
| 장바구니 상품 카드 안쪽 여백 |  | `cart.png` |
| 주문 요약 카드 안쪽 여백 |  | `cart.png` |
| 상품 이미지 영역 비율 | 1:1 | `home.png` |

상품 이미지는 정사각형 영역 안에서 잘리지 않게 표시하며, 실제 이미지 비율을
억지로 채우지 않는다.

## 선

| 항목 | 값 | 쓰이는 곳 |
|---|---:|---|
| 기본 선 굵기 |  | 카드·입력·버튼 테두리 |
| 구분선 굵기 |  | 행과 영역 사이 구분선 |
| 강조 선 굵기 |  | 선택된 옵션 아래 파란 선 |
| 선 스타일 | 실선 | 화면에 보이는 카드·구분선 |

## 모서리

| 항목 | 값 | 쓰이는 곳 |
|---|---:|---|
| 카드 모서리 반경 |  | 장바구니 상품 카드, 주문 요약 카드 |
| 입력·버튼 모서리 반경 |  | 검색창, 버튼, 수량 조절 상자 |
| 배지 모서리 반경 |  | 배송·적립 배지 |
| 상품 이미지 모서리 반경 |  | 상품 이미지 영역 |

화면에서는 직각에 가까운 카드와 입력 영역, 약간 둥근 선택 상자·배지가 함께
보이지만 정확한 반경 값은 확정할 수 없어 비워 둔다.

## 화면별 적용

### 상품 목록 (`home.png`)

- 왼쪽에 필터·카테고리 영역, 오른쪽에 상품 목록을 둔다.
- 상품은 격자로 배치하고 이미지, 상품명, 가격, 배송·보조 정보를 순서대로 둔다.
- 목록 위에 제목과 정렬 영역을 둔다.

### 상품 상세 (`detail.png`)

- 왼쪽에 큰 상품 이미지, 오른쪽에 상품 정보와 옵션을 둔다.
- 옵션은 가로 선택 영역과 행 목록으로 보여 준다.
- 선택된 옵션은 파란색과 굵은 글씨로 구분한다.
- 오른쪽 가장자리에 장바구니 요약 영역이 보인다.

### 장바구니 (`cart.png`)

- 상단에 로고와 장바구니 제목, 오른쪽에 주문 단계 표시를 둔다.
- 왼쪽에는 선택 가능한 상품 목록, 오른쪽에는 주문 예상 금액을 둔다.
- 상품 카드와 주문 요약 카드는 흰색 바탕과 얇은 테두리로 구분한다.
- 상품을 선택하지 않은 주문 버튼은 비활성 상태로 보인다.

## Behance 참고 화면에서 반영한 방향

Behance의 현재 탐색 화면에서 확인되는 시각 원칙만 반영한다. Behance의 로고,
브랜드명, 프로젝트 이미지, 작품명, 작가 정보와 커뮤니티 기능은 사용하지 않는다.

- 상단에는 검색과 핵심 탐색 링크를 한 줄에 두고, 콘텐츠와 분리되는 얇은 경계를
  둔다.
- 본문은 넓은 흰 여백 안에 반복되는 콘텐츠 단위를 배치한다.
- 카드가 여러 개 반복되더라도 각 카드의 이미지와 제목이 먼저 읽히게 한다.
- 탐색 선택은 한 줄의 정렬·분류 컨트롤로 간결하게 두고, 복잡한 사이드바는
  추가하지 않는다.
- 검색 입력과 선택 컨트롤은 화면에서 보이는 둥근 형태를 참고하되, 정확한
  모서리 반경은 확인할 수 없어 공란으로 유지한다.
- 목록 화면에서 보조 정보는 제목과 이미지보다 작게 두며, 수치나 상태는 필요한
  경우에만 표시한다. 우리 쇼핑몰에서는 가격과 분류만 `SHOP.md` 데이터로
  표시한다.
- 화면 폭이 줄어들면 반복 카드 수를 줄이고 탐색 컨트롤을 여러 줄로 감싼다.

## 선택한 상품 목록 디자인 — 편집형 비대칭 그리드

- 상품 목록은 균일한 카드만 반복하지 않고, 상품 이미지의 크기와 위치에
  차이를 둔 비대칭 편집형 그리드로 구성한다.
- 상품 카드는 기존 상품 데이터의 이미지·상품명·설명·가격을 그대로 사용한다.
  새로운 배너, 별점, 리뷰, 브랜드 정보는 추가하지 않는다.
- 카드 사이 간격은 이 문서에 정의된 상품 목록 카드 사이 간격을 사용한다.
- 색상은 기존 역할을 유지한다. 기본 배경은 `#ffffff`, 이미지 영역은
  보조 라벤더 `#f4f0fb`, 본문은 강한 본문색 `#111111`, 가격 강조는 핵심
  보라색 `#4b00a5`를 사용한다.
- 화면 폭이 줄어들면 비대칭 배치를 해제하고 상품 카드를 2열, 1열 순서로
  줄여 상품 정보가 잘리지 않도록 한다.
- 정확한 카드 크기 비율과 새로운 간격·모서리·그림자 값은 화면에서 확정하지
  않았으므로 추가하지 않는다.

## 인기상품 라벨과 균일 카드

- 상품 목록 카드는 모두 동일한 크기로 표시해 스크롤 중 더 많은 상품이 보이게
  한다. 기존 비대칭 카드 크기는 사용하지 않는다.
- 전체 구매 수량 상위 5개 상품의 이미지 오른쪽 상단에 `top1`~`top5` 라벨을
  표시한다.
- 라벨은 순위별로 다음 기존 테마 색상을 사용한다.
  - `top1`: 분홍 포인트 `#ed4b86`
  - `top2`: 핵심 보라색 `#4b00a5`
  - `top3`: 보조 노랑 `#ffb800`
  - `top4`: 연보라 보조색 `#d9c8f0`
  - `top5`: 보조 라벤더 `#f4f0fb`
- `top4`와 `top5` 라벨의 글자는 강한 본문색 `#111111`으로 표시한다.
- 상품 카드 사이 간격과 상하 여백은 기존 정의값을 유지하며, 별도의 새 그림자나
  장식은 추가하지 않는다.

## 선택한 헤더 디자인 — Behance 탐색형 헤더

- 헤더는 로고 없이 검색창을 중심에 두고, 오른쪽에 핵심 탐색 메뉴와 사용자
  동작 버튼을 배치한다.
- 검색창은 입력 배경 `#f4f4f7`, 본문은 강한 본문색 `#111111`을 사용한다.
- 핵심 행동 버튼은 분홍 포인트 `#ed4b86`을 사용하고, 일반 메뉴와 보조 버튼은
  강한 본문색 `#111111`과 흰색 배경을 사용한다.
- 버튼과 검색창은 Behance 화면에서 보이는 둥근 형태를 참고한다. 정확한 모서리
  반경과 높이는 화면에서 확정되지 않았으므로 새 값을 지정하지 않는다.
- 헤더의 한글·영문 텍스트는 기존 지정 글꼴인 `Noto Sans KR`을 사용한다.
- 헤더의 정확한 글꼴 크기와 간격은 화면에서 확정되지 않았으므로 기존 값을
  유지하고 임의의 수치를 추가하지 않는다.
- Behance의 로고, 브랜드명, 계정 정보와 사이트 전용 문구는 사용하지 않는다.

Behance 화면의 정확한 색상 코드, 글꼴 이름, 간격과 모서리 수치는 페이지의
렌더링 화면만으로 확정할 수 없는 항목은 공란으로 남겼다.

첨부 이미지에서 위 색상 대표값을 확인했다. 색상은
이미지의 렌더링·압축에 따라 달라질 수 있으므로 실제 구현 전 공식 색상값과
폰트 파일 또는 지정명을 확인해야 한다.
